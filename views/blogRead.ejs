<!DOCTYPE html>
<html lang="ja">
	<head>
		<%- include( "./head" ); %>
		<title>blogReadページ</title>
	</head>
	<body>
		<%- include( "./header" ); %>
		<main>
			<div class="blog-read">
				<h1><%= singleBlog.title %></h1>
				<img src="/public/images<%= singleBlog.image %>" alt="blog-image"/>
				<p><%= singleBlog.textBody %></p>
			</div>
			<div class="bottom">
				<% if ( session !== undefined ) { %>
					<a href="/update/<%= singleBlog._id %>" class="update-button">編集</a>
					<a href="/delete/<%= singleBlog._id %>" class="delete-button">削除</a>
				<% } %>
			</div>
		</main>
		<%- include( "./footer" ); %>
	</body>
</html>
